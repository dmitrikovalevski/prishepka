Рекомендуемый метод для каждой модели.
При работе с формой модели он всегда будет возвращать в указанное место пути по имени.
В этом случае имя пути (name='post_detail')

    def get_absolute_url(self):
        return reverse('post_detail', args=[(self.id)])